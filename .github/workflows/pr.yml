# filled out in Phase 12
name: PR checks
on:
  pull_request:
    branches: [main, develop]
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "placeholder — real checks added in Phase 12"
